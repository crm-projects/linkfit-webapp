group = 'com.server.storefront'
version = '1.0-SNAPSHOT'

dependencies {
    implementation project(path: ':utils')
    implementation project(path: ':core')
    implementation project(path: ':auth-service')
}

test {
    useJUnitPlatform()
}